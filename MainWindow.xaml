<Window x:Class="AP_HA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JPBM-BodyViewer" Width="800" Height="600" MinWidth="400" MinHeight="400" Icon="/AP-HA;component/icon/flughafensicherheit.ico" MouseWheel="Window_MouseWheel">

    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <MenuItem Header="_Datei">
                <MenuItem Name="menuOpenFolder" Header="_Stapel laden" Click="menuOpenFolder_Click" />
                <MenuItem Name="menuCloseFolder" Header="_Stapel Beenden" Click="menuCloseFolder_Click" IsEnabled="False" />
                <MenuItem Name="menuExitProgram" Header="_Beenden" Click="menuExitProgram_Click" />
            </MenuItem>
            <MenuItem Name="menuEdit" Header="_Bearbeiten">
                <MenuItem Name="menuCutStack" Header="_Stapelgröße" IsEnabled="False" >
                    <MenuItem Name="menuCutBeforeCursor" Header="_Bilder links entfernen" Click="menuCutBeforeCursor_Click" IsEnabled="False" />
                    <MenuItem Name="menuCutAfterCursor" Header="_Bilder rechts entfernen" Click="menuCutAfterCursor_Click"  IsEnabled="True"/>
                    <MenuItem Name="menuBackToOriginalCut" Header="_Ganzen Stapel anzeigen" Click="menuBackToOriginalCut_Click"  IsEnabled="False"/>
                </MenuItem>
            </MenuItem>           
            <MenuItem Header="_Einstellungen">
                <MenuItem Name="menuMouseSettings" Header="_Maus" />
            </MenuItem>           
        </Menu>

        <ToolBarTray Name="toolBarTray1" Background="#15000040" DockPanel.Dock="Top" Height="40">
            <ToolBar Name="toolBar1">
                <Button Content="ZoomIn" Name="btnZoomIn" Click="btnZoomIn_Click" />
                <Button Content="ZoomOut" Name="btnZoomOut" Click="btnZoomOut_Click" />
                <Button Content="Verschieben" Name="btnMove" Click="btnMove_Click" />
            </ToolBar>
        </ToolBarTray>
        
        <StatusBar DockPanel.Dock="Bottom" Name="statusBar" Background="LightGray">
            <StatusBarItem>
                <TextBlock>Bereit</TextBlock>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Name="debugTxtBox" Foreground="Red" TextAlignment="Right">
                    DebugTextBox
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
        
        <Slider DockPanel.Dock="Bottom" Height="23" Name="stackSlider" Margin="2,0,2,0" Background="#15000040" ValueChanged="stackSlider_ValueChanged" IsEnabled="False" SmallChange="1" Foreground="Blue" BorderBrush="Red" />

        <StackPanel DockPanel.Dock="Left" Orientation="Vertical" Background="#15000040">
            <Label Margin="2">Helligkeit</Label>
            <Slider Name="BrightnessSlider" Height="23" Width="100" Margin="2" Value="1" ValueChanged="BrightnessSlider_ValueChanged" Maximum="2" IsSnapToTickEnabled="True" TickFrequency="0.01" IsEnabled="False" />
            <Label Margin="2">Kontrast</Label>
            <Slider Name="ContrastSlider" Height="23" Width="100" Margin="2" Value="1" ValueChanged="ContrastSlider_ValueChanged" Maximum="2" IsSnapToTickEnabled="True" TickFrequency="0.01" IsEnabled="False" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Right" Orientation="Vertical" Background="#15000040">
            <Label Margin="2">Innereienmarkierer</Label>
            <ListBox Height="100" Name="marksListBox" Width="120" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding}">
            </ListBox>
        </StackPanel>

        <ScrollViewer Name="scrollViewer" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" PreviewMouseLeftButtonDown="scrollViewer_PreviewMouseLeftButtonDown" MouseMove="scrollViewer_MouseMove" MouseLeftButtonUp="scrollViewer_MouseLeftButtonUp" PreviewMouseLeftButtonUp="scrollViewer_MouseLeftButtonUp">
            <Border BorderBrush="Black" BorderThickness="1,1,1,1">
                <Canvas Name="canvas" Background="#fff" Focusable="True" MouseLeftButtonDown="canvas_MouseLeftButtonDown">
                    <Image Name="imgControl" Height="{Binding Path=ActualHeight, ElementName=canvas}" Width="{Binding Path=ActualWidth, ElementName=canvas}"  Stretch="Uniform" />
                </Canvas>
            </Border>
        </ScrollViewer>
    </DockPanel>
</Window>
